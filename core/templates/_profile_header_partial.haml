.row
  .span3
    .well.picture
      - if speaker.photo
        %img{src:"{{MEDIA_URL}}{{ speaker.photo}}"}
      - else
        %i.icon-user

  .span9
    {{ speaker.about_me }}

.features
  .row
    .span3
      %h2 Known for
      - if speaker.tags.all
        %ul.expert-area
          - for tag in speaker.tags.all
            %li.label.label-info {{tag}}
      - else
        No User Tags

    .span3
      %h2 Links
      - if speaker.userlink_set.all
        - for link in speaker.userlink_set.all
          %p
            %a{href:"{{link.url_target}}", target:"_blank"}
              My {{ link.type_name|title }} Account
      - else
        No Outside Links

    .span3
      %h2 Followers
      - if followers
        - for profile in followers
          %p
            %a{href:"/speaker/{{profile.user.username}}"}
              {{ profile.user.get_full_name }}
      - else
        No Followers

    .span3
      %h2 Following
      - if following
        - for profile in following
          %p
            %a{href:"/speaker/{{profile.user.username}}"}
              {{ profile.user.get_full_name }}
      - else
        Not Following Anyone
